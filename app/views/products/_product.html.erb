<div class="well">
  <h4><%= product.name %></h4>

  <%= link_to image_tag(product.image.thumb.url), product.image.url, class: 'fancybox' %>

  <p>
    Cena: <%= number_to_currency product.price, unit: '€', format: '%n %u', separator: ',' %>
  </p>

  <%= button_to 'Pridať do košíka', cart_items_path(product_id: product.id), class: "btn btn-primary btn-xs" %>
</div>