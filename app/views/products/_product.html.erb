<div class='col-md-3'>
  <div class='well product'>
    <h3><%= product.name %></h3>

    <%= link_to image_tag(product.image.thumb.url), product.image.url, class: 'product-image fancybox' %>

    <p class='product-price'>
      Cena: <span><%= number_to_currency product.price, unit: '€', format: '%n %u', separator: ',' %></span>
    </p>

    <%= button_to 'Pridať do košíka', cart_items_path(product_id: product.id), class: "btn btn-primary" %>
  </div>
</div>